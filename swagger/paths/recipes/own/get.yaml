tags:
  - Recipes
summary: Get user's own recipes
description: Retrieve paginated recipes created by the authorized user.
operationId: getOwnRecipes
security:
  - bearerAuth: []
parameters:
  - in: query
    name: page
    schema:
      type: integer
      default: 1
    description: Page number (starts from 1)
  - in: query
    name: perPage
    schema:
      type: integer
      default: 12
    description: Number of recipes per page
responses:
  '200':
    description: Successfully retrieved user's own recipes
    content:
      application/json:
        schema:
          type: object
          properties:
            status:
              type: integer
              example: 200
            message:
              type: string
              example: Successfully fetched own recipes!
            data:
              type: object
              properties:
                recipes:
                  type: array
                  items:
                    $ref: '../../../components/schemas/recipe.yaml'
                page:
                  type: integer
                  example: 2
                perPage:
                  type: integer
                  example: 12
                totalResults:
                  type: integer
                  example: 50
                totalPages:
                  type: integer
                  example: 5
                hasPreviousPage:
                  type: boolean
                  example: true
                hasNextPage:
                  type: boolean
                  example: true
  '401':
    $ref: '../../../components/responses/401.yaml'
  '404':
    $ref: '../../../components/responses/404.yaml'
